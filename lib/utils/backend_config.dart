// backend_config.dart

class BackendConfig {
  // Base URL for your PHP backend.
  // Use 'http://10.0.2.2/backend_new' for Android Emulator.
  // Replace with your local machine's IP address (e.g., 'http://192.168.1.x/backend_new')
  // to run on a physical Android device connected to the same network.
  // For iOS simulator or device, you might need to use your local IP as well.
  static const String baseUrl = 'http://192.168.1.6/backend_new';
}
